export interface TechIconProps {
    code?: string;
    /** the foreground color to appear for the logo */
    logoColor?: 'white' | 'black',
    /** `style` in hex code */
    style?: 'for-the-bade' | string;
    /** `color` in hex format */
    color?: string;
}
