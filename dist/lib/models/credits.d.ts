export interface ICredits {
    /** `amount`: the number of credits (integer) the customer has */
    amount?: number;
    /** `totalEarned`: the total number of credits the customer has earned */
    totalEarned?: number;
    /** `totalSpent`: the total number of credits the customer has spent */
    totalSpent?: number;
}
